# ========== Python (Backend) ==========
__pycache__/
*.pyc
*.pyo
.venv/
*.egg-info/

# ========== Node.js (Frontend) ==========
node_modules/

# ========== Build output ==========
dist/
build/

# ========== ChromaDB ==========
chroma_data/

# ========== 정책 문서 (PDF/DOCX — 로컬 배치, 커밋 제외) ==========
shopping_mall/backend/ai/docs/

# ========== Environment variables ==========
.env
.env.local
!.env.example

# ========== Secrets / credentials (never commit) ==========
# 인증 쿠키 덤프 파일 (curl --cookie-jar 등이 생성)
cookies.txt
*.cookies
# 개인키 / 인증서 (운영 환경 비밀)
*.key
*.pem
*.p12
*.pfx
# 응답에 포함된 시크릿이 들어갈 수 있는 디렉터리
secrets/
.secrets/

# ========== IDE / Editor ==========
.vscode/
.idea/
*.swp
*.swo

# ========== OS ==========
.DS_Store
Thumbs.db

# ========== Claude Code ==========
.claude/

# ========== MCP config ==========
.mcp.json

# ========== bkit ==========
.bkit/

# ========== Logs ==========
*.log

# ========== Benchmark ==========
# 벤치마킹 파일에 불과하므로 해당 파일의 소유주(JunePark2018)가 업로드 제외 처리함
backend/benchmark_openrouter.py
backend/benchmark_stt.py

# ========== Lock files (uv) ==========
# uv.lock는 재현성을 위해 커밋 권장 — 제외하지 않음

# ========== 로컬 실행 스크립트 ==========
start.cmd

# ========== 로컬 도구의 DB credentials 설정 (커밋 절대 금지) ==========
# 일부 GUI 도구(pgAdmin shortcut 등)가 repo 루트에 settings.json 으로 DB 비밀번호를 남김.
# 코드는 이 파일을 참조하지 않음 — 백엔드 DB 설정은 backend/.env 의 DATABASE_URL 사용.
/settings.json
