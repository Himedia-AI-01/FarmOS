# FarmOS Ralph Progress Log

| Iter | Date | Area | Title | Commit | Outcome |
|------|------|------|-------|--------|---------|
| 1 | 2026-05-04 | backend | Crop-aware weather risk advisory tool | f6435a7 | New deterministic analyzer + `get_weather_risk_advisory` tool wired into FARM_DATA + ORCHESTRATOR. 13 unit tests pass; backend imports clean; frontend build clean. |
| 2 | 2026-05-04 | backend | Frost-shield fast-path pattern | 96a5575 | Added `_FROST_RE` + `_format_frost` to fast_path.py; "오늘 밤 서리?" 류 질의를 LLM 없이 weather_alerts 의 frost 어드바이저리만 추려 응답. 9 신규 + 13 기존 단위 테스트 통과; 백엔드/프론트엔드 빌드 통과. |
| 3 | 2026-05-04 | backend | General extreme-weather risk fast-path | 15c54c7 | `_GENERAL_RISK_RE` + `_format_general_risks` + `_select_risk_kinds` 로 폭염/호우/강풍/한파/특보 단답형 질의를 weather_alerts engine 으로 즉답. 키워드별 advisory kind 화이트리스트 필터링; fallback 경로는 critical+warning 만 노출해 info 잡음 차단. 13 신규 + 23 기존 = 36/36 단위 테스트 통과; 백엔드 import + 프론트엔드 build clean. |
| 4 | 2026-05-04 | backend | Drought advisory threshold | 3a47e2b | `_detect_drought` + `drought` kind 추가 — 향후 daily_forecasts 첫 항목부터 강수<1mm 연속일을 세어 5일 warning / 7일 critical advisory 생성. 결측·강우·현재 강수 발생 시 보수적 중단. 10개 작물 drought crop_hint 추가, sort key 도 "향후 Nd" 슬롯 부여. 8 신규 + 13 기존 = 21/21 + fast_path 23/23 단위 테스트 통과; 백엔드 import + 프론트엔드 build clean. |
| 5 | 2026-05-04 | backend | Snow / 폭설 advisory threshold + fast-path | a48e513 | weather_alerts 에 `_is_snow_sky` + `snow` kind 추가 (daily.sky 가 눈/진눈깨비/눈날림/비/눈 + precip ≥ 5mm warning, ≥ 10mm critical, 1mm≈1cm 보수 가정). 10개 작물 snow crop_hint(시설하우스 적설하중·가지 부러짐·월동 멀칭). fast_path `_GENERAL_RISK_RE` 에 폭설/대설/적설 추가 + `_RISK_KEYWORD_TO_KINDS` 에 snow 매핑(❄️ 폭설/적설 위험). 단독 "눈" 은 비-기상 어휘 충돌로 의도적 배제. 10 신규 + 21 기존 = 31/31 weather_alerts + 5 신규 + 23 기존 = 28/28 fast_path 단위 테스트 통과; 백엔드 import + 프론트엔드 build clean. |
| 6 | 2026-05-04 | backend | Diurnal temperature swing (일교차) advisory threshold | e672968 | weather_alerts 에 `_DIURNAL_RANGE_*` 임계 + `temp_swing` kind 추가 — daily.tmax-tmin ≥16℃ warning, ≥20℃ critical, frost/heatwave 와 독립 발화 (절댓값 임계 무관). 8개 작물 temp_swing crop_hint (사과·배 열과, 포도 착색, 토마토·딸기 갈라짐, 오이 곡과, 고추 낙화, 배추 추대 가속, 벼 등숙기 야간저온). 결측 (tmin or tmax 없음) 은 보수적 무발화. 8 신규 + 31 기존 = 39/39 weather_alerts + 28/28 fast_path 단위 테스트 통과; 백엔드 import + 프론트엔드 build clean. |
| 7 | 2026-05-04 | backend | Diurnal swing (일교차) fast-path keyword | 1b607e6 | iter-6 의 `temp_swing` advisory 를 LLM 우회 즉답 경로로 노출 — `_GENERAL_RISK_RE` 에 `일교차` 추가 + `_RISK_KEYWORD_TO_KINDS` 에 `(("일교차",), {"temp_swing"}, "🌡️ 일교차 (큰 기온차) 위험")` 매핑. "기온차"는 `_WEATHER_RE` 의 "기온" prefix 와 충돌해 의도적 제외, "온도차"는 실내/구역간 의미 모호로 거부. 6 신규 + 28 기존 = 34/34 fast_path + 39/39 weather_alerts 단위 테스트 통과; 백엔드 import + 프론트엔드 build clean. |
| 8 | 2026-05-04 | backend | Cold wave (한파/혹한) explicit advisory kind | 996353a | weather_alerts 에 `_COLD_WAVE_*` 임계 + `cold_wave` kind 추가 — tmin ≤-10℃ warning, ≤-15℃ critical (KMA 한파특보 -12℃ 기준 보수적 채택). frost 와 동시 발화 허용해 서로 다른 액션 셋(살수 vs 난방·동파 방지)을 분리 노출. 8개 작물 cold_wave crop_hint (사과·배·포도 휴면기 동해, 시설 토마토/오이/딸기 난방·수막 동결, 마늘·양파·배추 월동). fast_path `_RISK_KEYWORD_TO_KINDS` 한파/혹한 매핑에 `cold_wave` 1개 항목 추가 (frost 유지 — backward compat). 9 신규 + 39 기존 = 48/48 weather_alerts + 34/34 fast_path 단위 테스트 통과 (기존 `test_select_risk_kinds_cold_wave_maps_to_frost` 는 새 매핑 반영해 두 테스트로 분기). 백엔드 import + 프론트엔드 build clean. |
| 9 | 2026-05-04 | backend | Wind chill (체감온도) advisory for outdoor work scheduling | 6a402ee | weather_alerts 에 `_compute_wind_chill` (Environment Canada 공식, KMA 동일 모델) + `wind_chill` kind 추가 — tmin + wmax 조합으로 보수적 worst-case 추정, ≤-10℃ warning, ≤-20℃ critical (동상 의학 가이드라인). 공식 유효구간(T≤10℃, V≥1.34m/s) 밖에선 None 반환해 외삽 금지. 7개 노지 작물(사과·배·포도·고추·배추·마늘·양파) wind_chill crop_hint — 야외 전정·수확·점검 작업 시간대 조정 + 손가락·발 동상 예방 액션. 시설(토마토/오이/딸기)·수도(벼) 는 의도적 미정의 (실내 작업·동계 휴경). frost·cold_wave 와 독립 발화 — tmin=-15·wmax=8 케이스에서 세 advisory 가 동시 발화하는 단위 테스트로 검증. 11 신규 + 48 기존 = 59/59 weather_alerts + 34/34 fast_path 단위 테스트 통과; 백엔드 import + 프론트엔드 build clean. |
| 10 | 2026-05-04 | backend | Pest pressure window (응애·진딧물) advisory | 9a50473 | weather_alerts 에 `mites_window` (tmax≥28℃ + humidity≤60%) + `aphid_window` (20≤tmax≤27℃ + humidity≤50%) info kind 추가 — 곰팡이성 fungal_humidity 와 정반대 시그널이라 동시 발화 불가능 (의도적). 임계는 보수적으로 잡아 일상적 봄·가을 날씨로는 점화하지 않게 하고 진짜 호조 패턴(폭염 직전 / 가뭄 동반)에서만 정보성 신호. 7개 작물 mites_window crop_hint (사과·배·포도·토마토·오이·딸기·고추 — 잎 뒷면 점박이·차응애 예찰·천적 방사) + 8개 작물 aphid_window crop_hint (사과·배·포도·토마토·오이·딸기·고추·배추 — 황색 점착 트랩·바이러스 매개 차단). humidity 결측 시 보수적 무발화. 11 신규 + 59 기존 = 70/70 weather_alerts + 34/34 fast_path 단위 테스트 통과; 백엔드 import + 프론트엔드 build clean. |
