"""CLI scripts for FarmOS backend (eval, maintenance, ad-hoc ops).

Listed in pyproject.toml's [project.scripts] so they install as console commands
via `uv sync`. Each module here exposes a `main()` entry point.
"""
